# TickerBrief mobile

See the [root README](../../README.md) for PowerShell setup, startup and checks, and the [release runbook](../../docs/RELEASE.md) for EAS/TestFlight.

Use `npm.cmd ci`, then `npm.cmd start`. Set `EXPO_PUBLIC_API_URL` to the accessible backend URL. Report snapshots and notes live only on this device.